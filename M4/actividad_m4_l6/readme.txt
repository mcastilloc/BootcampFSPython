Diferencias entre write() y append():
- write() sobrescribe el contenido del archivo.
- append() agrega contenido al final del archivo existente.

Ventaja de usar 'with open(...)':
- Cierra automáticamente el archivo al terminar, evitando errores.
- Hace el código más limpio y seguro.
